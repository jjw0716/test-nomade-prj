# NomadKR 개선 실행 계획

## 실행 계획 요약

- **핵심 목적:** NomadKR 홈페이지 UI/UX 간소화 및 필터·카드 구조 개선
- **전체 작업 성격:** 프런트엔드 리팩토링 + 데이터 모델 수정 (DB 없음, 가짜 데이터 사용)
- **주요 영향 파일:** `src/lib/data.ts`, `src/components/home/*`, `src/components/layout/Navbar.tsx`

---

## Phase 1 — 불필요 페이지 및 네비게이션 정리

홈페이지와 인증 페이지만 남기고 불필요한 페이지와 네비게이션 링크를 제거합니다.

### 태스크 1. 불필요한 페이지 및 파일 정리

- **목적:** 홈(`/`), 로그인(`/login`), 회원가입(`/signup`), `/auth/signout` 외 모든 페이지 제거
- **범위:** `src/app/` 하위 불필요 라우트 확인 및 삭제
- **산출물:** 4개 라우트만 남은 `src/app/` 디렉토리
- **의존성:** 없음

#### 수정/개선 항목
- [x] `src/app/` 하위에 홈·인증 외 페이지 존재 여부 확인 후 삭제

---

### 태스크 2. 네비게이션 링크 제거 (인증/프로필 버튼만 유지)

- **목적:** Navbar에서 도시 탐색·밋업·커뮤니티·코워킹 등 페이지 이동 링크 삭제
- **범위:** `src/components/layout/Navbar.tsx`, `src/components/layout/MobileMenu.tsx`
- **산출물:** 인증 관련 버튼만 남은 Navbar, MobileMenu
- **의존성:** 없음

#### 수정/개선 항목
- [x] `Navbar.tsx` — `navLinks` 배열 및 데스크톱 nav 링크 렌더링 제거
- [x] `MobileMenu.tsx` — 동일 `navLinks` 관련 UI 제거
- [x] 로그인 / 회원가입 / 로그아웃 버튼은 유지

### Phase 1 검증
- [x] 홈페이지(`/`), 로그인(`/login`), 회원가입(`/signup`) 접근 정상 동작
- [x] 그 외 라우트 접근 시 404 또는 리다이렉트
- [x] Navbar에 도시 탐색·밋업·커뮤니티·코워킹 링크 없음
- [x] 로그인/회원가입 버튼 정상 표시, 로그인 후 로그아웃 버튼 표시

---

## Phase 2 — 데이터 모델 확장

필터 조건에 맞는 필드를 City 인터페이스에 추가하고, 11개 도시 데이터를 업데이트합니다.

### 태스크 3. 데이터 모델 확장 (필터 필드 추가)

- **목적:** `City` 인터페이스에 `budget`, `district`, `environment`, `bestSeason` 필드 추가
- **범위:** `src/lib/data.ts` — `City` 인터페이스 수정
- **산출물:** 확장된 City 타입이 정의된 `src/lib/data.ts`
- **의존성:** 없음

#### 수정/개선 항목
- [x] `budget: "100만원이하" | "100~200만원" | "200만원이상"` 필드 추가
- [x] `district: "수도권" | "경상도" | "전라도" | "강원도" | "제주도" | "충청도"` 필드 추가
- [x] `environment: ("자연친화" | "도심선호" | "카페작업" | "코워킹 필수")[]` 배열 필드 추가
- [x] `bestSeason: ("봄" | "여름" | "가을" | "겨울")[]` 배열 필드 추가

---

### 태스크 4. 가짜 데이터 수정 (각 도시에 필터 필드 값 추가)

- **목적:** 11개 도시 각각에 `budget`, `district`, `environment`, `bestSeason` 값 할당
- **범위:** `src/lib/data.ts` — `cities` 배열 각 항목 수정
- **산출물:** 모든 도시에 4개 필터 필드가 채워진 `cities` 배열
- **의존성:** 태스크 3

#### 수정/개선 항목
- [x] 제주시 — `budget: "100~200만원"`, `district: "제주도"`, `environment: ["자연친화","카페작업"]`, `bestSeason: ["봄","가을"]`
- [x] 부산 — `budget: "100~200만원"`, `district: "경상도"`, `environment: ["도심선호","카페작업"]`, `bestSeason: ["봄","가을"]`
- [x] 서울 — `budget: "200만원이상"`, `district: "수도권"`, `environment: ["도심선호","코워킹 필수"]`, `bestSeason: ["봄","가을"]`
- [x] 강릉 — `budget: "100만원이하"`, `district: "강원도"`, `environment: ["자연친화","카페작업"]`, `bestSeason: ["여름","가을"]`
- [x] 전주 — `budget: "100만원이하"`, `district: "전라도"`, `environment: ["자연친화","카페작업"]`, `bestSeason: ["봄","가을"]`
- [x] 대전 — `budget: "100~200만원"`, `district: "충청도"`, `environment: ["도심선호","코워킹 필수"]`, `bestSeason: ["봄","가을"]`
- [x] 인천 송도 — `budget: "200만원이상"`, `district: "수도권"`, `environment: ["도심선호","코워킹 필수"]`, `bestSeason: ["봄","여름"]`
- [x] 속초 — `budget: "100만원이하"`, `district: "강원도"`, `environment: ["자연친화"]`, `bestSeason: ["여름","가을"]`
- [x] 여수 — `budget: "100만원이하"`, `district: "전라도"`, `environment: ["자연친화","카페작업"]`, `bestSeason: ["여름"]`
- [x] 경주 — `budget: "100만원이하"`, `district: "경상도"`, `environment: ["자연친화"]`, `bestSeason: ["봄","가을"]`
- [x] 통영 — `budget: "100만원이하"`, `district: "경상도"`, `environment: ["자연친화"]`, `bestSeason: ["봄","여름"]`

### Phase 2 검증
- [x] TypeScript 컴파일 오류 없음 (`npx tsc --noEmit`)
- [x] 11개 도시 모두 `budget`, `district`, `environment`, `bestSeason` 필드 보유
- [x] `environment`, `bestSeason` 각각 최소 1개 값 이상 배열로 보유

---

## Phase 3 — UI 개편 (필터·카드·정렬)

FilterBar를 새 필터 조건으로 전면 교체하고, CityCard 구조를 개편하며, 도시 리스트를 좋아요 순으로 정렬합니다.

### 태스크 5. FilterBar 교체 (새 필터 조건으로 전면 개편)

- **목적:** 기존 정렬/태그 필터를 예산·지역·환경·최고계절 필터로 교체
- **범위:** `src/components/home/FilterBar.tsx` 전면 수정
- **산출물:** 새 필터 조건이 적용된 FilterBar 컴포넌트
- **의존성:** 태스크 3

#### 수정/개선 항목
- [x] 기존 정렬 버튼(노마드 점수, 저렴한 순, 인터넷 속도) 제거
- [x] 기존 태그 필터(바다, 카페많음, 저렴) 제거
- [x] 그리드/맵 뷰 토글 제거
- [x] **예산** 필터 버튼 추가 — `100만원이하`, `100~200만원`, `200만원이상`
- [x] **지역** 필터 버튼 추가 — `전체`, `수도권`, `경상도`, `전라도`, `강원도`, `제주도`, `충청도`
- [x] **환경** 필터 버튼 추가 — `자연친화`, `도심선호`, `카페작업`, `코워킹 필수` (복수 선택 가능)
- [x] **최고 계절** 필터 버튼 추가 — `봄`, `여름`, `가을`, `겨울` (복수 선택 가능)
- [x] 지역 "전체" 선택 시 지역 필터 초기화
- [x] 필터 상태를 상위 컴포넌트(`page.tsx`)로 전달하는 props/콜백 설계

---

### 태스크 6. CityCard 구조 개편

- **목적:** 별점 제거, 필터 정보 Key-Value 표시, 좋아요/싫어요 인터랙션 구현
- **범위:** `src/components/home/CityCard.tsx` 수정
- **산출물:** 인터랙티브 좋아요/싫어요 및 필터 정보가 표시된 CityCard 컴포넌트
- **의존성:** 태스크 4

#### 수정/개선 항목
- [x] `Stars` 컴포넌트 및 별점 표시 UI 제거
- [x] `score` 값 표시 제거
- [x] 카드 바디에 필터 필드 Key-Value 표시 추가
  - [x] `예산: 100~200만원`
  - [x] `지역: 제주도`
  - [x] `환경: 자연친화, 카페작업`
  - [x] `최고 계절: 봄, 가을`
- [x] 좋아요 버튼 — 클릭 시 아이콘 초록색으로 변경 + 숫자 +1, 재클릭 시 원상 복구
- [x] 싫어요 버튼 — 클릭 시 아이콘 빨간색으로 변경 + 숫자 +1, 재클릭 시 원상 복구
- [x] 좋아요·싫어요 동시 활성화 불가 (한 쪽 선택 시 반대쪽 해제)
- [x] `useState`로 로컬 상태 관리 (DB 미사용)
- [x] "자세히 보기" 버튼 존재 시 삭제
- [x] `Link` 래퍼 제거 (카드 클릭 시 페이지 이동 없도록)

---

### 태스크 7. CityCardGrid 정렬 및 제목 변경, 필터 연동

- **목적:** 제목 변경, 좋아요 순 정렬, FilterBar 필터 상태 수신하여 카드 필터링
- **범위:** `src/components/home/CityCardGrid.tsx`, `src/app/page.tsx` 수정
- **산출물:** 좋아요 순 정렬 + 필터 연동된 CityCardGrid
- **의존성:** 태스크 5, 태스크 6

#### 수정/개선 항목
- [x] 섹션 제목 `'추천 도시 목록'` → `'도시 리스트'`로 변경
- [x] `cities` 배열을 `likes` 내림차순 정렬하여 렌더링
- [x] `page.tsx`에서 `useState`로 필터 상태 관리
- [x] FilterBar에 필터 상태 및 변경 콜백 props 전달
- [x] CityCardGrid에 필터 상태 props 전달하여 도시 필터링 적용

### Phase 3 검증
- [x] FilterBar에 예산(3개), 지역(7개), 환경(4개), 최고계절(4개) 버튼 표시
- [x] 필터 선택 시 해당 조건에 맞는 도시만 카드 표시
- [x] 지역 "전체" 선택 시 모든 도시 표시
- [x] 섹션 제목이 '도시 리스트'로 표시
- [x] 도시 카드가 좋아요 내림차순 정렬 (서울 203 → 제주 142 → 부산 118 순)
- [x] 카드에 별점(★) 없음
- [x] 카드에 예산/지역/환경/최고계절 Key-Value 표시
- [x] 좋아요 버튼 클릭 시 초록색으로 변경 + 숫자 +1
- [x] 싫어요 버튼 클릭 시 빨간색으로 변경 + 숫자 +1
- [x] 좋아요·싫어요 동시 활성화 불가
- [x] "자세히 보기" 버튼 없음
- [x] 카드 클릭 시 페이지 이동 없음
